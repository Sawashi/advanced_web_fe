export enum EClassRole {
  OWNER = "owner",
  TEACHER = "teacher",
  STUDENT = "student",
}

export enum ETabName {
  Stream = "stream",
  People = "people",
  Students = "students",
  GradeStructure = "grade-structure",
  GradeBoard = "grade-board",
}
